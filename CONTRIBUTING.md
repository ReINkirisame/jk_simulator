# 协作约定

## 分支与提交

- `main` 只保存当前可以运行的版本。
- 每项工作使用独立分支。
- Commit 信息使用“类型: 内容”，例如：`fix: show trait choice result`。
- Pull Request 描述应写明：目的、改动文件、试玩方法、已知问题。

## 修改范围

- 内容修改优先发生在 `src/data/`。
- 通用规则修改在 `src/core/`，必须说明会影响哪些事件。
- 不要在修一个错字的同时重排整个文件。
- 不要删除 `legacy/` 中的原始版本，除非双方明确决定不再保留。

## 合并前检查

- [ ] 双击 `index.html` 能正常进入游戏。
- [ ] 本次修改涉及的分支亲自试玩过。
- [ ] 浏览器 Console 没有新增红色错误。
- [ ] 若电脑有 Node.js，`npm test` 通过。
- [ ] 改动了随机序列、事件ID或状态结构时，更新版本并说明存档兼容策略。
- [ ] 发布单文件版前运行 `npm run build`，确认使用最新源码生成。
- [ ] PR 没有混入无关文件或私人信息。

## 使用 AI

给 AI 一个明确、可验证的任务，并指出允许和禁止修改的文件。让 AI 报告测试结果，但仍需由人查看差异并试玩。

当前版本是0.5.0，已有三年基础流程。扩写前先读 DESIGN.md、ARCHITECTURE.md 和 RELEASE_0.5.0.md，区分已经实现的规则与路线图中的待设计内容。规则测试不等于真实浏览器视觉验收，报告时分别写明。
